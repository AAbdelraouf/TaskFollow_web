import React from 'react';
import Body from './Body';
import { Container } from '@/components';

const index = () => {
  return (
    <Container>
      <Body />
    </Container>
  );
};

export default index;
